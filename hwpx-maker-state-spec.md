# hwpx-maker 상태 명세서 (state-spec)

ai-service-ux 스킬 기준으로 작성. 화면: HWPX 문서 생성기 (단일 화면).

## 6대 화면 상태

| CASE | 상태 | 구현 내용 | 구현 위치 |
|---|---|---|---|
| 01 | 정상 작동 | 입력 → 생성 → 초안 미리보기 → 근거 표 → 보정 → 내려받기까지 끊김 없이 이어짐. 프로토타입에서는 클라이언트에서 초안을 조립하고 TXT로 내려받음(HWPX는 API 연결 후) | `#stateSuccess`, `buildDraft()` |
| 02 | 로딩 중 | 스피너 + 단계별 진행 메시지 4종 순환("구조 분석 → 서술식 변환 → 기호 규칙 → 패키징") + 소요 시간 안내(대기 화면에 10~20초 고지) + 생성 취소 버튼 | `#stateLoading`, `LOADING_STEPS` |
| 03 | 결과 없음/부실 입력 | 제목 미입력·핵심 내용 20자 미만 시 필드별 인라인 안내. 단순 차단이 아니라 "배경·무엇·언제 중 두 가지 이상"이라는 채우는 방법을 제시. 첫 오류 필드로 포커스 이동 | `validate()`, `.field-error` |
| 04 | 연산·네트워크 실패 | 실패 사유 + "입력 내용은 보존됨" 고지 + 즉시 재시도 버튼 + 반복 실패 시 우회법(2,000자 이하 분할) 안내. API 연결 시 catch에서 `showError()` 호출 | `#stateError`, `showError()` |
| 05 | 중복 입력 | 생성 중 제출 버튼 비활성화 + 라벨 "생성 중…" 변경 + submit 핸들러에서 이중 가드 | `setState()`, submit 핸들러 |
| 06 | 권한 없음 | 해당 없음(비로그인 공개 도구). 로그인·기관 인증을 도입하는 시점에 차단 화면 + 우회 경로(담당자 문의) 설계 필요 — 미결 항목으로 남김 | — |

## 사용성 결함 4종 처방

| # | 결함 | 처방 |
|---|---|---|
| A-1 | 기대치 미고지 | 히어로 아래 2단 안내 박스: "이 도구가 바로 해주는 것" vs "담당자가 확인해야 하는 것" |
| A-2 | 근거 없는 단정 | 결과에 ① 초안(자동 생성물) 고지 ② 최종 확정 주체(발행 기관 담당자 검토·결재) 명시 ③ 구성 근거 표(입력이 어느 항목에 반영됐는지 + 규칙 기반/AI 판단 구분) |
| A-3 | 막다른 골목 | 부실 입력 시 채우는 방법 안내(CASE 03), 실패 시 재시도·분할 우회(CASE 04), 파일 형식 오류 시 HWP→DOCX 변환법 안내 |
| A-4 | 수정 불가능한 수동성 | 보정 칩("더 간결하게"/"더 상세하게") + 즉시 반영 + "이전 버전으로" 되돌리기(히스토리 스택) |

## 검증 규칙

- 문서 제목: 필수, 공백 제외 1자 이상
- 핵심 내용: 필수, 공백 제외 20자 이상, 최대 4,000자(3,800자부터 카운터 경고색)
- 첨부 파일: .txt/.pdf/.docx만, 10MB 이하. 위반 시 사유 + 해결법 안내, 첨부 취소 버튼 제공

## API 연결 시 교체 지점

- `buildDraft()` → 생성 API 호출로 교체 (`startGenerate()` 내 TODO 주석)
- 실패 시 `showError(사유)` 호출로 CASE 04 진입
- 다운로드: TXT → HWPX 바이너리 응답으로 교체

## 미결 항목 (다음 버전에서 "안 할 것"이 아니라 "아직 안 한 것")

- CASE 06 권한 화면 (로그인 도입 시)
- 보정 칩 고도화: 현재는 프로토타입 수준(항목 수 가감). API 연결 시 재생성 파라미터로 전달
- 이용 지표 적재(D-3): 생성 횟수·실패율·보정 칩 사용률 로깅
